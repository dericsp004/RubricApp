package com.plummer.deric.rubricapp;

/**
 * Created by Deric on 6/7/17.
 */
import org.junit.Test;

import java.util.List;

import static org.junit.Assert.*;


public class AssignmentUnitTest {

    /*@Test
    public void CreateNewAssignmentWithStudentsTest () {
        Assignment assignment =  new Assignment(new Rubric());
        for (int i = 0; i < 10; i++) {
            assignment.addStudent("John", "Doe");
        }

        for (Student students : assignment.getStudents()) {
            assertEquals(assignment.getRubric(), students.getRubric());
        }
    }

    @Test
    public void SaveAssingmentTest () {
        Assignment assignment =  new Assignment(new Rubric());
        assignment.setAssignmentName("English 101 - Moby Dick Essay");
        //assignment.save(new Context());

        //Check assingment is saved correctly

    }

    @Test
    public void AddRemoveStudentTest() {
        boolean pass = false;
        Assignment assignment =  new Assignment("Midterm", new Rubric());
        for (Integer i = 0; i < 10; i++) {
            assignment.addStudent("John", i.toString());
        }

        assignment.removeStudent(1);
        boolean result1 = assignment.containsStudent("John", "1");
        assertFalse(result1);

        assignment.removeStudent("John", "5");
        boolean result2 = assignment.containsStudent("John", "5");
        assertFalse(result2);

        assignment.addStudent("Jane", "Doe");
        boolean result3 = assignment.containsStudent("Jane", "Doe");
        assertTrue(result3);

        System.out.println(assignment.getData());
    }*/
}
